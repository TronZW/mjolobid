services:
  - type: web
    name: mjolobid
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: chmod +x deploy.sh && ./deploy.sh
    disk:
      name: media-disk
      mountPath: /var/media
      sizeGB: 1
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: False
      - key: DJANGO_SETTINGS_MODULE
        value: mjolobid.settings_render
      - key: DATABASE_URL
        fromDatabase:
          name: mjolobid-db
          property: connectionString
      - key: SEED_ON_DEPLOY
        value: "false"
      - key: CREATE_SUPERUSER_ON_DEPLOY
        value: "false"
      - key: MEDIA_ROOT
        value: /var/media
    healthCheckPath: /

databases:
  - name: mjolobid-db
    plan: free
